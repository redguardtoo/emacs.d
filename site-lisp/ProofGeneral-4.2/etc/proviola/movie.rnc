default namespace = ""

start =
  element movie {
    element film {
      element frame {
        attribute frameNumber { xsd:integer },
      	element command { text },
	element response { text }
     }
    }+
  }
