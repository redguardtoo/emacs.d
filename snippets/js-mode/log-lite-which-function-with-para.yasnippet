# -*- coding: utf-8; mode: snippet -*-
# name: console.log function&paras (lite)
# key: lwf
# contributor: Chen Bin <chenbin DOT sh AT gmail>
# --
console.log('${1:`(my-which-function)`} => ', `(mapconcat (lambda (i) (format "'%s=', %s" i i)) (split-string (car kill-ring) ",[ \s\n]*") ", ")`);
