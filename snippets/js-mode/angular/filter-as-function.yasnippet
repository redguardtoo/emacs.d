# -*- mode: snippet -*-
# name: var rlt = $filter('filter')(srcArray, function(item) { ... });
# group: angular
# key: afilter
# --
${1:dstArray} = $filter('filter')(${2:srcArray}, function(item) {
  // boolean or null means *item* is removed from result
  // or else just return the item
  return item.${3:prop} !== $0;
});
